# ── Parser (carsensor.net scraper) ───────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── DB bridge (save_to_db.py imports app.models / app.database) ───────────────
sqlalchemy>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pydantic>=2.7.0
pydantic-settings>=2.2.0

# ── Auth (app.security uses pwdlib) ──────────────────────────────────────────
pwdlib[bcrypt]>=0.2.1
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
